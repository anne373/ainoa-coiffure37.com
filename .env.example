# Vercel Postgres (depuis votre dashboard Vercel → Storage → Postgres → .env.local)
# OU via : vercel env pull .env.local
POSTGRES_PRISMA_URL=""
POSTGRES_URL_NON_POOLING=""

# Vercel Blob (depuis votre dashboard Vercel → Storage → Blob)
BLOB_READ_WRITE_TOKEN=""

# URL publique du site
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
